---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: default-backend
  labels:
    app: default-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: haproxy-network-ingress-default-backend
  template:
    metadata:
      labels:
        app: haproxy-network-ingress-default-backend
    spec:
      containers:
        - name: default-backend
          image: haproxy:2.0
          ports:
            - name: healthz
              containerPort: 80
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /healthz
              port: healthz
          readinessProbe:
            httpGet:
              path: /healthz
              port: healthz
          volumeMounts:
          - mountPath: /usr/local/etc/haproxy/
            name: network-ingress-configuration
            readOnly: true
      volumes:
      - configMap:
          defaultMode: 420
          name: network-ingress-configuration
        name: network-ingress-configuration
